/target
/e2e/target
*/dist
**/dist
.claude
egui_components/assets/
# `nix build` output symlinks — they point into /nix/store and dangle
# everywhere else, which crashes the GitHub Pages Jekyll builder.
/result
/result-*
